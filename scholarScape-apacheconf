ProxyRequests Off

<Proxy *>
        Options +FollowSymLinks
        AllowOverride All
        order allow,deny
        allow from all
</Proxy>

<Location /scholarScape/>
# you might have to change localhost and 6969 port according to the host condifugation of the twisted server
# have a look in config.json on scholarscape root directory:
        ProxyPass http://localhost:6969/
        ProxyPassReverse http://localhost:6969/
</Location>
